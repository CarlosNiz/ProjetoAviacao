# Projeto Engemap

Teste prático — sistema de gerenciamento de Missões (Projetos) e Faixas de
coordenadas para execução por aeronave.

## Estrutura

- `engemap-backend/` — API REST em Python (FastAPI)
- `engemap-frontend/` — aplicação desktop (a ser adicionada)

## Como rodar o backend

Ver `engemap-backend/README.md`.